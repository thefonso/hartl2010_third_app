<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <%= csrf_meta_tag %>
    <%= render 'layouts/stylesheets' %>
	<!-- <%= javascript_include_tag :default %> THIS IS WRONG NOW FOR RUBY 3.1...IT'S RIGHT FOR 3.0...YOU WILL NOT FIGURE THIS OUT ON YOUR OWN....HASH ROCKET DUDES AT CODE AND COFFEE HELPED ME FIGURE THIS ONE OUT..THANK YOU Mr. DAVE FROM HASHROCKET!..-->
    <%= javascript_include_tag "application" %>
  </head>
  <body>
      <div class="container"> 
          <%= render 'layouts/header' %>
      <section class="round">
        <% flash.each do |key, value| %>
          <div class="flash <%= key %>"><%= value %></div>
        <% end %>
        <%= yield %>
      </section>
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
